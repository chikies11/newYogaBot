# ???????????? ???? ?????? ??? Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/yogabot}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ????????? Supabase
supabase.url=https://uiunhoxqwtbrtzcsmrny.supabase.co
supabase.key=${SUPABASE_KEY:your_supabase_anon_key_here}

# Telegram Bot Configuration
bot.username=${BOT_USERNAME:}
bot.token=${BOT_TOKEN:}
bot.path=${BOT_PATH:}

# App Configuration
app.channelId=${CHANNEL_ID:}
app.adminId=${ADMIN_ID:}

# Spring Boot Settings
spring.main.allow-circular-references=true
spring.main.lazy-initialization=true

# Logging
logging.level.org.example=DEBUG
logging.level.org.springframework.web=INFO

# Spring Boot Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Server
server.port=${PORT:8080}