# ??????????? Spring Boot
spring.main.lazy-initialization=true
spring.main.log-startup-info=false
spring.main.allow-circular-references=true

# Database Configuration ? HikariCP
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d3e2m8bipnbc73c7jl6g-a/yogabot_db_edt5}
spring.datasource.username=${DATABASE_USER:yogabot_user}
spring.datasource.password=${DATABASE_PASSWORD:AH7nGSee5ZqoG2kEj3LZ6kq2SWILZVLt}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.pool-name=YogaBotPool
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.leak-detection-threshold=60000

# ??????????? Tomcat
server.tomcat.max-threads=50
server.tomcat.min-spare-threads=5
server.connection-timeout=30s
server.port=${PORT:8080}

# Telegram Bot Configuration
bot.username=${BOT_USERNAME:}
bot.token=${BOT_TOKEN:}
bot.path=${BOT_PATH:}

# App Configuration
app.channelId=${CHANNEL_ID:}
app.adminId=${ADMIN_ID:}

# ???????????
logging.level.org.example=DEBUG
logging.level.com.zaxxer.hikari=INFO

# Spring Boot Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always